<!-- Button trigger modal -->
<button class="btn btn-danger" data-toggle="modal" data-target="#deletePost{{ post.id }}ModalCenter">
  <i class="fas fa-trash"></i>
</button>

<!-- Modal -->
<div class="modal fade" id="deletePost{{ post.id }}ModalCenter" tabindex="-1" role="dialog" aria-labelledby="deletePost{{ post.id }}ModalCenterTitle" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deletePost{{ post.id }}ModalLongTitle">Suppression de l'article <span class="font-italic">{{ post.title }}</span></h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                La suppression de l'article <span class="font-italic">{{ post.title }}</span> entraînera la suppression des images qui lui sont associés et sera irréversible.
                Voulez-vous vraiment supprimer cet article ?
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Annuler</button>
                <!-- Button trigger modal -->
                <button type="button" class="btn btn-danger" data-dismiss="modal" data-toggle="modal" data-target="#deleteRelated{{ post.id }}ModalCenter">
                    Oui
                </button>
            </div>
        </div>
    </div>
</div>

<!-- Modal -->
<div class="modal fade" id="deleteRelated{{ post.id }}ModalCenter" tabindex="-1" role="dialog" aria-labelledby="deleteRelated{{ post.id }}ModalCenterTitle" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deleteRelated{{ post.id }}ModalLongTitle">Voulez-vous également supprimer les commentaires associés à cet article ?</h5>
                <button type="button" class="close btn-success" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-footer">
                <div class="row">
                    <form method="post" action="{{ path('posts_delete', {'id': post.id}) }}" class="col-6">
                        <input type="hidden" name="delete_related" value=false>
                        <input type="hidden" name="_method" value="DELETE">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post.id) }}">
                        <button class="btn btn-warning">Supprimer l'article et ses images</button>
                    </form>
                    <form method="post" action="{{ path('posts_delete', {'id': post.id}) }}" class="col-6">
                        <input type="hidden" name="delete_related" value=true>
                        <input type="hidden" name="_method" value="DELETE">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post.id) }}">
                        <button class="btn btn-danger">Supprimer les commentaires aussi</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

